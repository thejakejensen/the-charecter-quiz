$(document).ready(function(){
  $("#charecterButton").click(function(event) {
    var foodAnswer = $("#food").val()
    var movieAnswer = $("#movie").val()
    var colorAnswer = $("color")

    event.preventDefault();
  });
});
